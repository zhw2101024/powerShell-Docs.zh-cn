---
ms.date: 05/17/2018
keywords: powershell, 核心
title: PowerShell 6.0 的已知问题
ms.openlocfilehash: ce40a1925e564fbd2c661e70ec36d3842d915dfe
ms.sourcegitcommit: 47becf2823ece251a7264db2387bb503cf3abaa9
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 10/19/2018
ms.locfileid: "49450990"
---
# <a name="known-issues-for-powershell-60"></a><span data-ttu-id="fab96-103">PowerShell 6.0 的已知问题</span><span class="sxs-lookup"><span data-stu-id="fab96-103">Known Issues for PowerShell 6.0</span></span>

## <a name="known-issues-for-powershell-on-non-windows-platforms"></a><span data-ttu-id="fab96-104">非 Windows 平台上 PowerShell 的已知问题</span><span class="sxs-lookup"><span data-stu-id="fab96-104">Known Issues for PowerShell on Non-Windows Platforms</span></span>

<span data-ttu-id="fab96-105">Linux 和 macOS 上的 PowerShell 的 Alpha 版本通常能正常运行，但确实存在一些明显的限制和可用性问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-105">Alpha releases of PowerShell on Linux and macOS are mostly functional but do have some significant limitations and usability issues.</span></span> <span data-ttu-id="fab96-106">Linux 和 macOS 上的 PowerShell 的 Beta 版本比 alpha 版本功能更加强大且稳定，但仍可能缺少一些功能，并可能包含 bug。</span><span class="sxs-lookup"><span data-stu-id="fab96-106">Beta releases of PowerShell on Linux and macOS are more functional and stable than alpha releases, but still may be lacking some set of features, and can contain bugs.</span></span> <span data-ttu-id="fab96-107">在某些情况下，这些问题只是尚未得到修复的 bug。</span><span class="sxs-lookup"><span data-stu-id="fab96-107">In some cases, these issues are simply bugs that haven't been fixed yet.</span></span> <span data-ttu-id="fab96-108">在其他情况下（对于 ls、cp 等的默认别名），我们正在寻求社区对我们所做选择的反馈。</span><span class="sxs-lookup"><span data-stu-id="fab96-108">In other cases (as with the default aliases for ls, cp, etc.), we are looking for feedback from the community regarding the choices we make.</span></span>

<span data-ttu-id="fab96-109">注意：由于许多基础子系统的相似性，Linux 和 macOS 上的 PowerShell 倾向于在功能和 bug 方面共享相同的成熟度级别。</span><span class="sxs-lookup"><span data-stu-id="fab96-109">Note: Due to the similarities of many underlying subsystems, PowerShell on Linux and macOS tend to share the same level of maturity in both features and bugs.</span></span> <span data-ttu-id="fab96-110">除下文所述，本部分中的问题将适用于这两个操作系统。</span><span class="sxs-lookup"><span data-stu-id="fab96-110">Except as noted below, the issues in this section will apply to both operating systems.</span></span>

### <a name="case-sensitivity-in-powershell"></a><span data-ttu-id="fab96-111">PowerShell 中的区分大小写</span><span class="sxs-lookup"><span data-stu-id="fab96-111">Case-sensitivity in PowerShell</span></span>

<span data-ttu-id="fab96-112">PowerShell 一直以来均区分大小写，只有少数例外情况。</span><span class="sxs-lookup"><span data-stu-id="fab96-112">Historically, PowerShell has been uniformly case-insensitive, with few exceptions.</span></span> <span data-ttu-id="fab96-113">在类似于 UNIX 的操作系统上，文件系统基本都区分大小写，并且 PowerShell 遵守文件系统的标准；这通过多种方式显示，明显和非明显的方式。</span><span class="sxs-lookup"><span data-stu-id="fab96-113">On UNIX-like operating systems, the file system is predominantly case-sensitive and PowerShell adheres to the standard of the file system; this is exposed through a number of ways, obvious and non-obvious.</span></span>

#### <a name="directly"></a><span data-ttu-id="fab96-114">直接</span><span class="sxs-lookup"><span data-stu-id="fab96-114">Directly</span></span>

- <span data-ttu-id="fab96-115">在 PowerShell 中指定一个文件时，必须使用正确的大小写。</span><span class="sxs-lookup"><span data-stu-id="fab96-115">When specifying a file in PowerShell, the correct case must be used.</span></span>

#### <a name="indirectly"></a><span data-ttu-id="fab96-116">间接</span><span class="sxs-lookup"><span data-stu-id="fab96-116">Indirectly</span></span>

- <span data-ttu-id="fab96-117">如果脚本尝试加载一个模块，模块名称大小写不正确，则模块加载将失败。</span><span class="sxs-lookup"><span data-stu-id="fab96-117">If a script tries to load a module and the module name is not cased correctly, then the module load will fail.</span></span> <span data-ttu-id="fab96-118">如果该模块所引用的名称与实际文件名不匹配，则可能导致现有脚本出现问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-118">This may cause a problem with existing scripts if the name by which the module is referenced doesn't match the actual file name.</span></span>
- <span data-ttu-id="fab96-119">如果文件名大小写有误，则 Tab 自动补全将不会自动完成。</span><span class="sxs-lookup"><span data-stu-id="fab96-119">Tab-completion will not auto-complete if the file name case is wrong.</span></span> <span data-ttu-id="fab96-120">要完成的片段必须采用正确的大小写。</span><span class="sxs-lookup"><span data-stu-id="fab96-120">The fragment to complete must be cased properly.</span></span> <span data-ttu-id="fab96-121">（对于类型名称和类型成员完成，完成是不区分大小写的。）</span><span class="sxs-lookup"><span data-stu-id="fab96-121">(Completion is case-insensitive for type name and type member completions.)</span></span>

### <a name="ps1-file-extensions"></a><span data-ttu-id="fab96-122">.PS1 文件扩展名</span><span class="sxs-lookup"><span data-stu-id="fab96-122">.PS1 File Extensions</span></span>

<span data-ttu-id="fab96-123">PowerShell 脚本必须以 `.ps1` 结尾，以便解释器了解如何在当前进程中加载并运行它们。</span><span class="sxs-lookup"><span data-stu-id="fab96-123">PowerShell scripts must end in `.ps1` for the interpreter to understand how to load and run them in the current process.</span></span> <span data-ttu-id="fab96-124">在当前进程中运行脚本是 PowerShell 的预期常见行为。</span><span class="sxs-lookup"><span data-stu-id="fab96-124">Running scripts in the current process is the expected usual behavior for PowerShell.</span></span> <span data-ttu-id="fab96-125">可能会将 `#!` 幻数添加到不带 `.ps1` 扩展名的脚本，但这将导致该脚本在新的 PowerShell 实例中运行，从而在交换对象时阻止脚本正常运行。</span><span class="sxs-lookup"><span data-stu-id="fab96-125">The `#!` magic number may be added to a script that doesn't have a `.ps1` extension, but this will cause the script to be run in a new PowerShell instance preventing the script from working properly when interchanging objects.</span></span> <span data-ttu-id="fab96-126">（注意：这可能是从 `bash` 或其他 shell 执行 PowerShell 脚本时所期望的行为。）</span><span class="sxs-lookup"><span data-stu-id="fab96-126">(Note: this may be the desirable behavior when executing a PowerShell script from `bash` or another shell.)</span></span>

### <a name="missing-command-aliases"></a><span data-ttu-id="fab96-127">缺少命令别名</span><span class="sxs-lookup"><span data-stu-id="fab96-127">Missing command aliases</span></span>

<span data-ttu-id="fab96-128">在 Linux/macOS 上，已删除基本命令 `ls`、`cp`、`mv`、`rm`、`cat`、`man`、`mount`、`ps` 的“方便别名”。</span><span class="sxs-lookup"><span data-stu-id="fab96-128">On Linux/macOS, the "convenience aliases" for the basic commands `ls`, `cp`, `mv`, `rm`, `cat`, `man`, `mount`, `ps` have been removed.</span></span> <span data-ttu-id="fab96-129">在 Windows 上，为了方便用户，PowerShell 提供一组映射到 Linux 命令名的别名。</span><span class="sxs-lookup"><span data-stu-id="fab96-129">On Windows, PowerShell provides a set of aliases that map to Linux command names for user convenience.</span></span> <span data-ttu-id="fab96-130">已从 Linux/macOS 分发上的默认 PowerShell 中删除这些别名，以允许在不指定路径的情况下运行本机可执行文件。</span><span class="sxs-lookup"><span data-stu-id="fab96-130">These aliases have been removed from the default PowerShell on Linux/macOS distributions, allowing the native executable to be run without specifying a path.</span></span>

<span data-ttu-id="fab96-131">执行此操作既有优点，又有缺点。</span><span class="sxs-lookup"><span data-stu-id="fab96-131">There are pros and cons to doing this.</span></span> <span data-ttu-id="fab96-132">删除别名会向 PowerShell 用户公开本机命令体验，但会降低 shell 中的功能，因为本机命令会返回字符串，而不是对象。</span><span class="sxs-lookup"><span data-stu-id="fab96-132">Removing the aliases exposes the native command experience to the PowerShell user, but reduces functionality in the shell because the native commands return strings instead of objects.</span></span>

> [!NOTE]
> <span data-ttu-id="fab96-133">PowerShell 团队正在寻求此方面的反馈。</span><span class="sxs-lookup"><span data-stu-id="fab96-133">This is an area where the PowerShell team is looking for feedback.</span></span>
> <span data-ttu-id="fab96-134">首选解决方案是什么？</span><span class="sxs-lookup"><span data-stu-id="fab96-134">What is the preferred solution?</span></span> <span data-ttu-id="fab96-135">我们应保持原样还是重新添加方便别名？</span><span class="sxs-lookup"><span data-stu-id="fab96-135">Should we leave it as is or add the convenience aliases back?</span></span> <span data-ttu-id="fab96-136">请参阅[问题 #929](https://github.com/PowerShell/PowerShell/issues/929)。</span><span class="sxs-lookup"><span data-stu-id="fab96-136">See [Issue #929](https://github.com/PowerShell/PowerShell/issues/929).</span></span>

### <a name="missing-wildcard-globbing-support"></a><span data-ttu-id="fab96-137">缺少通配符（通配）支持</span><span class="sxs-lookup"><span data-stu-id="fab96-137">Missing Wildcard (globbing) Support</span></span>

<span data-ttu-id="fab96-138">目前，PowerShell 只对 Windows 上的内置 cmdlet 和 Linux 上的外部命令或二进制文件及 cmdlet 做通配符扩展（通配）。</span><span class="sxs-lookup"><span data-stu-id="fab96-138">Currently, PowerShell only does wildcard expansion (globbing) for built-in cmdlets on Windows, and for external commands or binaries as well as cmdlets on Linux.</span></span> <span data-ttu-id="fab96-139">这意味着，类似 `ls
*.txt` 的命令将失败，因为不会展开星号以匹配文件名。</span><span class="sxs-lookup"><span data-stu-id="fab96-139">This means that a command like `ls
*.txt` will fail because the asterisk will not be expanded to match file names.</span></span> <span data-ttu-id="fab96-140">可以通过执行 `ls (gci *.txt | % name)` ，或更简单地使用等效于 `ls` 的 PowerShell 内置项来执行 `gci *.txt` 以解决此问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-140">You can work around this by doing `ls (gci *.txt | % name)` or, more simply, `gci *.txt` using the PowerShell built-in equivalent to `ls`.</span></span>

<span data-ttu-id="fab96-141">请参阅 [#954](https://github.com/PowerShell/PowerShell/issues/954) 提供有关如何改进 Linux/macOS 上通配体验的反馈。</span><span class="sxs-lookup"><span data-stu-id="fab96-141">See [#954](https://github.com/PowerShell/PowerShell/issues/954) to give us feedback on how to improve the globbing experience on Linux/macOS.</span></span>

### <a name="net-framework-vs-net-core-framework"></a><span data-ttu-id="fab96-142">.NET Framework 和 .NET Core Framework</span><span class="sxs-lookup"><span data-stu-id="fab96-142">.NET Framework vs .NET Core Framework</span></span>

<span data-ttu-id="fab96-143">Linux/macOS 上的 PowerShell 使用 .NET Core，即 Microsoft Windows 上的完整 .NET Framework 的子集。</span><span class="sxs-lookup"><span data-stu-id="fab96-143">PowerShell on Linux/macOS uses .NET Core which is a subset of the full .NET Framework on Microsoft Windows.</span></span> <span data-ttu-id="fab96-144">这非常重要，因为 PowerShell 提供对基础框架类型、方法等的直接访问。因此，在 Windows 上运行的脚本可能无法在非 Windows 平台上运行，因为框架之间存在差异。</span><span class="sxs-lookup"><span data-stu-id="fab96-144">This is significant because PowerShell provides direct access to the underlying framework types, methods, etc. As a result, scripts that run on Windows may not run on non-Windows platforms because of the differences in the frameworks.</span></span> <span data-ttu-id="fab96-145">有关 .NET Core Framework 的详细信息，请参阅 <https://dotnetfoundation.org/net-core></span><span class="sxs-lookup"><span data-stu-id="fab96-145">For more information about .NET Core Framework, see <https://dotnetfoundation.org/net-core></span></span>

<span data-ttu-id="fab96-146">随着 [.NET Standard 2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/) 的出现，.NET Core 2.0 将恢复存在于完整 .NET Framework 中的许多传统类型和方法。</span><span class="sxs-lookup"><span data-stu-id="fab96-146">With the advent of [.NET Standard2.0](https://blogs.msdn.microsoft.com/dotnet/2016/09/26/introducing-net-standard/), .NET Core 2.0 will bring back many of the traditional types and methods present in the full .NET Framework.</span></span> <span data-ttu-id="fab96-147">这意味着，PowerShell Core 将能够加载许多传统 Windows PowerShell 模块，而无需进行修改。</span><span class="sxs-lookup"><span data-stu-id="fab96-147">This means that PowerShell Core will be able to load many traditional Windows PowerShell modules without modification.</span></span> <span data-ttu-id="fab96-148">可以在[此处](https://github.com/PowerShell/PowerShell/projects/4)了解我们有关 .NET Standard 2.0 的工作。</span><span class="sxs-lookup"><span data-stu-id="fab96-148">You can follow our .NET Standard 2.0 related work [here](https://github.com/PowerShell/PowerShell/projects/4).</span></span>

### <a name="redirection-issues"></a><span data-ttu-id="fab96-149">重定向问题</span><span class="sxs-lookup"><span data-stu-id="fab96-149">Redirection Issues</span></span>

<span data-ttu-id="fab96-150">任何平台上的 PowerShell 中均不支持输入重定向。</span><span class="sxs-lookup"><span data-stu-id="fab96-150">Input redirection is not supported in PowerShell on any platform.</span></span>
[<span data-ttu-id="fab96-151">问题 #1629</span><span class="sxs-lookup"><span data-stu-id="fab96-151">Issue #1629</span></span>](https://github.com/PowerShell/PowerShell/issues/1629)

<span data-ttu-id="fab96-152">使用 `Get-Content` 将文件内容写入管道。</span><span class="sxs-lookup"><span data-stu-id="fab96-152">Use `Get-Content` to write the contents of a file into the pipeline.</span></span>

<span data-ttu-id="fab96-153">在使用默认的 UTF-8 编码时，重定向的输出将包含 Unicode 字节顺序标记 (BOM)。</span><span class="sxs-lookup"><span data-stu-id="fab96-153">Redirected output will contain the Unicode byte order mark (BOM) when the default UTF-8 encoding is used.</span></span> <span data-ttu-id="fab96-154">在使用不应包含 BOM 的实用工具或附加到文件时，BOM 将导致出现问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-154">The BOM will cause problems when working with utilities that do not expect it or when appending to a file.</span></span> <span data-ttu-id="fab96-155">使用 `-Encoding Ascii` 写入 ASCII 文本（它不是 Unicode，将不包含 BOM）。</span><span class="sxs-lookup"><span data-stu-id="fab96-155">Use `-Encoding Ascii` to write ASCII text (which, not being Unicode, will not have a BOM).</span></span>

> [!Note]
> <span data-ttu-id="fab96-156">请参阅 [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71)，向我们提供有关在所有平台上改进 PowerShell Core 编码体验的反馈。</span><span class="sxs-lookup"><span data-stu-id="fab96-156">see [RFC0020](https://github.com/PowerShell/PowerShell-RFC/issues/71) to give us feedback on improving the encoding experience for PowerShell Core across all platforms.</span></span> <span data-ttu-id="fab96-157">我们致力于支持不带 BOM 的 UTF-8，以及可能不断变化的各种跨平台的 cmdlet 的编码默认值。</span><span class="sxs-lookup"><span data-stu-id="fab96-157">We are working to support UTF-8 without a BOM and potentially changing the encoding defaults for various cmdlets across platforms.</span></span>

### <a name="job-control"></a><span data-ttu-id="fab96-158">作业控制</span><span class="sxs-lookup"><span data-stu-id="fab96-158">Job Control</span></span>

<span data-ttu-id="fab96-159">Linux/macOS 上的 PowerShell 中不支持作业控制。</span><span class="sxs-lookup"><span data-stu-id="fab96-159">There is no job-control support in PowerShell on Linux/macOS.</span></span>
<span data-ttu-id="fab96-160">`fg` 和 `bg` 命令不可用。</span><span class="sxs-lookup"><span data-stu-id="fab96-160">The `fg` and `bg` commands are not available.</span></span>

<span data-ttu-id="fab96-161">目前，可以使用跨所有平台执行操作的 [PowerShell 作业](/powershell/module/microsoft.powershell.core/about/about_jobs)。</span><span class="sxs-lookup"><span data-stu-id="fab96-161">For the time being, you can use [PowerShell jobs](/powershell/module/microsoft.powershell.core/about/about_jobs) which do work across all platforms.</span></span>

### <a name="remoting-support"></a><span data-ttu-id="fab96-162">远程处理支持</span><span class="sxs-lookup"><span data-stu-id="fab96-162">Remoting Support</span></span>

<span data-ttu-id="fab96-163">目前，PowerShell Core 支持在 macOS 和 Linux 上经过基本身份验证以及在 Linux 上经过基于 NTLM 的身份验证的通过 WSMan 的 PowerShell 远程处理 (PSRP)。</span><span class="sxs-lookup"><span data-stu-id="fab96-163">Currently, PowerShell Core supports PowerShell Remoting (PSRP) over WSMan with Basic authentication on macOS and Linux, and with NTLM-based authentication on Linux.</span></span> <span data-ttu-id="fab96-164">（不支持基于 Kerberos 的身份验证。）</span><span class="sxs-lookup"><span data-stu-id="fab96-164">(Kerberos-based authentication is not supported.)</span></span>

<span data-ttu-id="fab96-165">在 [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) 存储库中完成基于 WSMan 的远程处理工作。</span><span class="sxs-lookup"><span data-stu-id="fab96-165">The work for WSMan-based remoting is being done in the [psl-omi-provider](https://github.com/PowerShell/psl-omi-provider) repo.</span></span>

<span data-ttu-id="fab96-166">PowerShell Core 还支持所有平台（Windows、macOS 和 Linux）上通过 SSH 的 PowerShell 远程处理 (PSRP)。</span><span class="sxs-lookup"><span data-stu-id="fab96-166">PowerShell Core also supports PowerShell Remoting (PSRP) over SSH on all platforms (Windows, macOS, and Linux).</span></span> <span data-ttu-id="fab96-167">虽然目前在生产中不支持此功能，但可以在[此处](../core-powershell/ssh-remoting-in-powershell-core.md)了解有关此设置的详细信息。</span><span class="sxs-lookup"><span data-stu-id="fab96-167">While this is not currently supported in production, you can learn more about setting this up [here](../core-powershell/ssh-remoting-in-powershell-core.md).</span></span>

### <a name="just-enough-administration-jea-support"></a><span data-ttu-id="fab96-168">Just-Enough-Administration (JEA) 支持</span><span class="sxs-lookup"><span data-stu-id="fab96-168">Just-Enough-Administration (JEA) Support</span></span>

<span data-ttu-id="fab96-169">Linux/macOS 上的 PowerShell 目前无法创建约束管理 (JEA) 远程处理终结点。</span><span class="sxs-lookup"><span data-stu-id="fab96-169">The ability to create constrained administration (JEA) remoting endpoints is not currently available in PowerShell on Linux/macOS.</span></span> <span data-ttu-id="fab96-170">此功能目前不在 6.0 的范围内，我们将在后 6.0 中考虑此功能，因为它需要大量的设计工作。</span><span class="sxs-lookup"><span data-stu-id="fab96-170">This feature is currently not in scope for 6.0 and something we will consider post 6.0 as it requires significant design work.</span></span>

### <a name="sudo-exec-and-powershell"></a><span data-ttu-id="fab96-171">`sudo`、`exec` 和 PowerShell</span><span class="sxs-lookup"><span data-stu-id="fab96-171">`sudo`, `exec`, and PowerShell</span></span>

<span data-ttu-id="fab96-172">由于 PowerShell 在内存（如 Python 或 Ruby）中运行大多数命令，不能直接将 sudo 与 PowerShell 内置项一起使用。（当然，可以从 sudo 运行 `pwsh`。）如果需要使用 sudo（例如，`sudo Set-Date 8/18/2016`）从 PowerShell 内运行 PowerShell cmdlet，则要执行 `sudo pwsh Set-Date 8/18/2016`。</span><span class="sxs-lookup"><span data-stu-id="fab96-172">Because PowerShell runs most commands in memory (like Python or Ruby), you can't use sudo directly with PowerShell built-ins. (You can, of course, run `pwsh` from sudo.) If it is necessary to run a PowerShell cmdlet from within PowerShell with sudo, for example, `sudo Set-Date 8/18/2016`, then you would do `sudo pwsh Set-Date 8/18/2016`.</span></span> <span data-ttu-id="fab96-173">同样，不能直接执行 PowerShell 内置项。</span><span class="sxs-lookup"><span data-stu-id="fab96-173">Likewise, you can't exec a PowerShell built-in directly.</span></span> <span data-ttu-id="fab96-174">而必须执行 `exec pwsh item_to_exec`。</span><span class="sxs-lookup"><span data-stu-id="fab96-174">Instead you would have to do `exec pwsh item_to_exec`.</span></span>

<span data-ttu-id="fab96-175">此问题目前作为 [#3232](https://github.com/PowerShell/PowerShell/issues/3232) 的一部分进行跟踪。</span><span class="sxs-lookup"><span data-stu-id="fab96-175">This issue is currently being tracked as part of [#3232](https://github.com/PowerShell/PowerShell/issues/3232).</span></span>

### <a name="missing-cmdlets"></a><span data-ttu-id="fab96-176">缺少 Cmdlet</span><span class="sxs-lookup"><span data-stu-id="fab96-176">Missing Cmdlets</span></span>

<span data-ttu-id="fab96-177">通常可用于 PowerShell 的大多数命令 (cmdlet) 在 Linux/macOS 上不可用。</span><span class="sxs-lookup"><span data-stu-id="fab96-177">A large number of the commands (cmdlets) normally available in PowerShell are not available on Linux/macOS.</span></span> <span data-ttu-id="fab96-178">在许多情况下，这些命令在这些平台（例如，类似于注册表的 Windows 特定的功能）上没有意义。</span><span class="sxs-lookup"><span data-stu-id="fab96-178">In many cases, these commands make no sense on these platforms (e.g. Windows-specific features like the registry).</span></span> <span data-ttu-id="fab96-179">类似于服务控制命令 (Get/Start/Stop-Service) 的其他命令存在，但不起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-179">Other commands like the service control commands (Get/Start/Stop-Service) are present, but not functional.</span></span> <span data-ttu-id="fab96-180">将来的版本将纠正这些问题，修复损坏的 cmdlet 并随着事件的推移添加一些新的 cmdlet。</span><span class="sxs-lookup"><span data-stu-id="fab96-180">Future releases will correct these problems, fixing the broken cmdlets and adding new ones over time.</span></span>

### <a name="command-availability"></a><span data-ttu-id="fab96-181">命令可用性</span><span class="sxs-lookup"><span data-stu-id="fab96-181">Command Availability</span></span>

<span data-ttu-id="fab96-182">下表列出了 Linux/macOS 上的 PowerShell 中已知不起作用的命令。</span><span class="sxs-lookup"><span data-stu-id="fab96-182">The following table lists commands that are known not to work in PowerShell on Linux/macOS.</span></span>

|<span data-ttu-id="fab96-183">命令</span><span class="sxs-lookup"><span data-stu-id="fab96-183">Commands</span></span>|<span data-ttu-id="fab96-184">操作状态</span><span class="sxs-lookup"><span data-stu-id="fab96-184">Operational State</span></span>|<span data-ttu-id="fab96-185">注释</span><span class="sxs-lookup"><span data-stu-id="fab96-185">Notes</span></span>|
|--------|-----------------|-----|
|<span data-ttu-id="fab96-186">`Get-Service`、`New-Service`、`Restart-Service`、`Resume-Service`、`Set-Service`、`Start-Service`、`Stop-Service`、`Suspend-Service`</span><span class="sxs-lookup"><span data-stu-id="fab96-186">`Get-Service`, `New-Service`, `Restart-Service`, `Resume-Service`, `Set-Service`, `Start-Service`, `Stop-Service`, `Suspend-Service`</span></span>|<span data-ttu-id="fab96-187">不可用。</span><span class="sxs-lookup"><span data-stu-id="fab96-187">Not Available.</span></span>|<span data-ttu-id="fab96-188">不会识别这些命令。</span><span class="sxs-lookup"><span data-stu-id="fab96-188">These commands will not be recognized.</span></span> <span data-ttu-id="fab96-189">应在将来的版本中解决此问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-189">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="fab96-190">`Get-Acl`、`Set-Acl`</span><span class="sxs-lookup"><span data-stu-id="fab96-190">`Get-Acl`, `Set-Acl`</span></span>|<span data-ttu-id="fab96-191">不可用。</span><span class="sxs-lookup"><span data-stu-id="fab96-191">Not available.</span></span>|<span data-ttu-id="fab96-192">不会识别这些命令。</span><span class="sxs-lookup"><span data-stu-id="fab96-192">These commands will not be recognized.</span></span> <span data-ttu-id="fab96-193">应在将来的版本中解决此问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-193">This should be fixed in a future release.</span></span>|
|<span data-ttu-id="fab96-194">`Get-AuthenticodeSignature`、`Set-AuthenticodeSignature`</span><span class="sxs-lookup"><span data-stu-id="fab96-194">`Get-AuthenticodeSignature`, `Set-AuthenticodeSignature`</span></span>|<span data-ttu-id="fab96-195">不可用。</span><span class="sxs-lookup"><span data-stu-id="fab96-195">Not available.</span></span>|<span data-ttu-id="fab96-196">不会识别这些命令。</span><span class="sxs-lookup"><span data-stu-id="fab96-196">These commands will not be recognized.</span></span> <span data-ttu-id="fab96-197">应在将来的版本中解决此问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-197">This should be fixed in a future release.</span></span>|
|`Wait-Process`|<span data-ttu-id="fab96-198">可用，无法正常工作。</span><span class="sxs-lookup"><span data-stu-id="fab96-198">Available, doesn't work properly.</span></span> |<span data-ttu-id="fab96-199">例如，“Start-Process gvim -PassThru</span><span class="sxs-lookup"><span data-stu-id="fab96-199">For example \`Start-Process gvim -PassThru</span></span> | <span data-ttu-id="fab96-200">Wait-Process”无效；它无法等待进程。</span><span class="sxs-lookup"><span data-stu-id="fab96-200">Wait-Process\` doesn't work; it fails to wait for the process.</span></span>|
|<span data-ttu-id="fab96-201">`Register-PSSessionConfiguration`、`Unregister-PSSessionConfiguration`、`Get-PSSessionConfiguration`</span><span class="sxs-lookup"><span data-stu-id="fab96-201">`Register-PSSessionConfiguration`, `Unregister-PSSessionConfiguration`, `Get-PSSessionConfiguration`</span></span>|<span data-ttu-id="fab96-202">可用，但不起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-202">Available but doesn't work.</span></span>|<span data-ttu-id="fab96-203">写入错误消息，指示命令不起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-203">Writes an error message indicating that the commands are not working.</span></span> <span data-ttu-id="fab96-204">应在将来的版本中解决这些问题。</span><span class="sxs-lookup"><span data-stu-id="fab96-204">These should be fixed in a future release.</span></span>|
|<span data-ttu-id="fab96-205">`Get-Event`、`New-Event`、`Register-EngineEvent`、`Register-WmiEvent`、`Remove-Event`、`Unregister-Event`</span><span class="sxs-lookup"><span data-stu-id="fab96-205">`Get-Event`, `New-Event`, `Register-EngineEvent`, `Register-WmiEvent`, `Remove-Event`, `Unregister-Event`</span></span>|<span data-ttu-id="fab96-206">可用，但没有可用的事件源。</span><span class="sxs-lookup"><span data-stu-id="fab96-206">Available but no event sources are available.</span></span>|<span data-ttu-id="fab96-207">PowerShell 事件命令存在，但与命令一起使用的大部分事件源（例如 System.Timers.Timer）在 Linux 上不可用，使得 Alpha 版本中的命令无效。</span><span class="sxs-lookup"><span data-stu-id="fab96-207">The PowerShell eventing commands are present but most of the event sources used with the commands (such as System.Timers.Timer) are not available on Linux making the commands useless in the Alpha release.</span></span>|
|`Set-ExecutionPolicy`|<span data-ttu-id="fab96-208">可用，但不起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-208">Available but doesn't work.</span></span>|<span data-ttu-id="fab96-209">返回消息，指示在此平台上不受支持。</span><span class="sxs-lookup"><span data-stu-id="fab96-209">Returns a message saying not supported on this platform.</span></span> <span data-ttu-id="fab96-210">执行策略是以用户为中心的“安全带”，有助于防止用户犯下代价昂贵的错误。</span><span class="sxs-lookup"><span data-stu-id="fab96-210">Execution policy is a user-focused "safety belt" that helps prevent the user from making expensive mistakes.</span></span> <span data-ttu-id="fab96-211">这不是安全边界。</span><span class="sxs-lookup"><span data-stu-id="fab96-211">It is not a security boundary.</span></span>|
|<span data-ttu-id="fab96-212">`New-PSSessionOption`、`New-PSTransportOption`</span><span class="sxs-lookup"><span data-stu-id="fab96-212">`New-PSSessionOption`, `New-PSTransportOption`</span></span>|<span data-ttu-id="fab96-213">可用，但 `New-PSSession` 不起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-213">Available but `New-PSSession` doesn't work.</span></span>|<span data-ttu-id="fab96-214">在 `New-PSSession` 起作用时，`New-PSSessionOption` 和 `New-PSTransportOption` 当前未被验证起作用。</span><span class="sxs-lookup"><span data-stu-id="fab96-214">`New-PSSessionOption` and `New-PSTransportOption` are not currently verified to work now that `New-PSSession` works.</span></span>|
